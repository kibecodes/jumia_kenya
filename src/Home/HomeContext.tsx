import React, { createContext } from "react"

export const HomeContext = createContext('access');

const 

export const ContextProvider = () => {
    return (
        <>
            <HomeContext.Provider>
                
            </HomeContext.Provider>
        </>
    )
}

